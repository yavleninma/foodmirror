import dotenv from "dotenv";

dotenv.config();

const defaultLlmContracts = {
  parseMeal: [
    "{",
    '  "items": [ {',
    '    "canonical_name": "",',
    '    "display_label": "",',
    '    "weight_g_mean": null,',
    '    "weight_g_min": null,',
    '    "weight_g_max": null,',
    '    "confidence": "LOW",',
    '    "confidence_reasons": null,',
    '    "barcode": null,',
    '    "user_kcal_per_100g": null,',
    '    "user_protein_per_100g": null,',
    '    "user_fat_per_100g": null,',
    '    "user_carbs_per_100g": null',
    "  } ],",
    '  "overall_confidence": 0.0,',
    '  "notes": null',
    "}",
  ].join("\n"),
  mealTitle: ["{", '  "title": null', "}"].join("\n"),
  foodReference: [
    "{",
    '  "items": [ {',
    '    "name": "",',
    '    "kcal_per_100g": null,',
    '    "protein_per_100g": null,',
    '    "fat_per_100g": null,',
    '    "carbs_per_100g": null,',
    '    "confidence": null,',
    '    "notes": null',
    "  } ]",
    "}",
  ].join("\n"),
  draftChat: [
    "{",
    '  "reply": null,',
    '  "items": [ {',
    '    "canonical_name": "",',
    '    "display_label": "",',
    '    "weight_g_mean": null,',
    '    "weight_g_min": null,',
    '    "weight_g_max": null,',
    '    "confidence": "LOW",',
    '    "confidence_reasons": null,',
    '    "barcode": null,',
    '    "user_kcal_per_100g": null,',
    '    "user_protein_per_100g": null,',
    '    "user_fat_per_100g": null,',
    '    "user_carbs_per_100g": null',
    "  } ],",
    '  "overall_confidence": 0.0,',
    '  "notes": null',
    "}",
  ].join("\n"),
};

const defaultLlmPrompts = {
  parseMeal: [
    "Верни один валидный JSON-объект строго по контракту Vision/Parsing. Никакого текста вне JSON. Никакого Markdown.",
    "{{contract}}",
    "canonical_name — каноническое название на английском lower_snake_case (стиль USDA): основной_ингредиент_подтип_приготовление (примеры: chicken_breast_cooked, apple, rice_cooked, cappuccino, latte_macchiato).",
    "display_label — отображаемое название на языке пользователя.",
    "overall_confidence — число от 0 до 1.",
    "confidence — только одно из: LOW, MEDIUM, HIGH.",
    "confidence_reasons — одна короткая строка на русском (причины через ';') или null. Без упоминаний ошибок, отсутствий, баз или справочников.",
    "Если причин нет — null. Не используй общие фразы вроде «диапазон построен по умолчанию».",
    "barcode — если на фото виден штрих-код (EAN-13/EAN-8/UPC) или пользователь ввёл числовой код (8-13 цифр), укажи его как строку. Иначе null.",
    "user_kcal_per_100g, user_protein_per_100g, user_fat_per_100g, user_carbs_per_100g — заполняй ТОЛЬКО если пользователь ЯВНО указал числовые значения калорий/БЖУ. Если «только углеводы» — user_protein=0, user_fat=0, user_carbs=kcal/4. Если не указал — null.",
    "Если есть текст, он приоритетнее изображения.",
    "Последние уточнения пользователя приоритетнее предыдущих сообщений.",
    "Если пользователь явно пишет, что что-то не ел/не пил/это чужое — исключи это из items, даже если видно на фото.",
    "Если ранее упоминалось, но затем отрицалось — исключи из items.",
    "Вес в граммах: если указываешь веса, соблюдай min <= mean <= max. Если известна только одна оценка — продублируй её в min/mean/max.",
    "Ширина диапазона веса должна отражать реальную неопределённость:",
    "- Стандартный напиток в чашке/стакане, штучный продукт, упакованная порция → узкий диапазон (±10-15%).",
    "- Составное блюдо, домашняя еда без указания веса, тарелка с неясным размером → широкий диапазон (±25-40%).",
    "Типичные порции для напитков: капучино/латте ~200 мл, эспрессо ~30 мл, американо ~150 мл, чай ~250 мл, стакан сока/воды ~250 мл.",
    "Типичные порции для блюд: тарелка супа ~300 мл, порция каши ~250 г, стандартная котлета ~100 г.",
    "Если еды не видно или данных мало — items: [], overall_confidence низкая, notes с короткой причиной.",
    "Если пользователь прислал только фото без текста — считай показанную еду съеденной полностью.",
    "Справка по порциям из базы данных (используй при оценке веса):",
    "{{portion_hints}}",
  ].join("\n"),
  mealTitle: [
    "Верни один валидный JSON-объект строго по контракту MealTitle. Никакого текста вне JSON. Никакого Markdown.",
    "{{contract}}",
    "title — строка или null.",
    "Название короткое, нейтральное, 2-5 слов.",
    "Название должно быть чётким: либо известное блюдо, либо перечисление основных компонентов.",
    "Запрещены слова: возможно, примерно, похоже, вероятно, может быть; неподтверждённо, неуверен/неуверенно, неточно/неточный/неточная, приблизительно, ориентировочно. В названии приёма пищи никогда не указывать степень уверенности или точности.",
    "Без оценок, без советов, без эмоций.",
    "Без обращений к пользователю напрямую.",
  ].join("\n"),
  foodReference: [
    "Верни один валидный JSON-объект строго по контракту FoodReference. Никакого текста вне JSON. Никакого Markdown.",
    "{{contract}}",
    "Используй значения на 100 грамм.",
    "Поля *_per_100g — числа или null (не строки).",
    "Если оценить нельзя — верни null поля и короткое примечание.",
    "Сохрани порядок и имена как в списке. Верни items той же длины, что и входной список.",
    "Список: {{items_json}}",
  ].join("\n"),
  draftChat: [
    "Диалог для разбора приёма пищи. Пользователь описывает еду текстом и/или фото. Цель — вычислить все продукты и их граммовку.",
    "",
    "{{contract}}",
    "",
    "reply — твоё сообщение пользователю: уточняющий вопрос (если нужно) или null, если достаточно данных.",
    "items — текущий список продуктов по разбору диалога.",
    "",
    "Правила:",
    "- Текст пользователя абсолютный приоритет. Если пишет «это не X, а Y» — меняй. Если «добавь масло 20г» — добавляй. Не противоречь.",
    "- canonical_name — английский lower_snake_case, в стиле USDA/справочника: основной_ингредиент_подтип_приготовление (например chicken_breast_cooked, apple, rice_cooked). display_label — на языке пользователя.",
    "- Фразы «без кожи», «без костей», «без шкурки» — описание способа приготовления продукта (курица без кожи, рыба без костей), не инструкция убрать ингредиент. display_label может быть лаконичным: «куриные ножки на пару» достаточно, без повторения «без кожи».",
    "",
    "Уточняющие вопросы (reply):",
    "- Если пользователь прислал фото еды — считай, что он это съел. Не спрашивай «съел ли всё», «вся ли тарелка», «доел ли».",
    "- Исключение: на фото видна упаковка соуса, приправы — можно спросить, сколько примерно добавили.",
    "- Не уточняй вес низкокалорийных продуктов: огурец, помидор, салат, зелень, редис — их вклад в калории незначителен.",
    "- Спрашивай только то, что реально влияет на калории: вес калорийных блюд, масло (если видно жарку), соусы, способ приготовления.",
    "- Лучше задай широкий диапазон веса, чем лишний вопрос. reply = null приоритетнее, чем вопрос «на всякий случай».",
    "- Если пользователь даёт конкретное исправление по одному продукту («картошку чуть меньше», «лосось 150 г» и т.п.) — примени только его и reply = null. Не переспрашивай то, что уже спрашивал. Пользователь делает точечную правку, а не отвечает на старые вопросы.",
    "- ВАЖНО: при уточнениях/добавлениях НЕ МЕНЯЙ веса продуктов, которые пользователь НЕ упоминал. Если пользователь пишет «добавь 50г брокколи» — добавь брокколи, но веса остальных продуктов оставь КАК БЫЛИ. Пересматривать чужие веса нельзя.",
    "",
    "Штрих-код (barcode):",
    "- Если на фото виден штрих-код (EAN-13/EAN-8/UPC) или пользователь ввёл числовой код (8-13 цифр), укажи его как строку в поле barcode. Иначе null.",
    "",
    "Поля user_* (пользовательские нутриенты):",
    "- user_kcal_per_100g, user_protein_per_100g, user_fat_per_100g, user_carbs_per_100g — заполняй ТОЛЬКО если пользователь ЯВНО указал числовые значения калорий или БЖУ. Если не указал — null.",
    "- ВЕРЬ ПОЛЬЗОВАТЕЛЮ БЕЗОГОВОРОЧНО. Если пользователь сказал «60 ккал» — user_kcal_per_100g = 60. Если сказал «10г жиров» — user_fat_per_100g = 10. Не подменяй данные пользователя своими оценками.",
    "- Если пользователь сказал «только углеводы» или «все из углеводов» — user_protein_per_100g = 0, user_fat_per_100g = 0, а user_carbs_per_100g = user_kcal / 4 (рассчитай). Аналогично для «только белки» и «только жиры».",
    "- Если пользователь указал ккал без явных БЖУ и без «только X» — заполни user_kcal_per_100g, остальные user_* оставь null. Доменная логика пропорционально масштабирует макросы.",
    "- Если пользователь в последующем сообщении уточняет «там ещё 10г жиров» — обнови user_fat_per_100g для этого продукта.",
    "{{point_correction_block}}",
    "- weight_g: min <= mean <= max.",
    "- Ширина диапазона веса = реальная неопределённость: стандартные напитки/штучные продукты — ±10-15%; составные/домашние блюда без чёткого размера — ±25-40%.",
    "- Типичные порции: капучино/латте ~200 мл, эспрессо ~30 мл, американо ~150 мл, чай ~250 мл, стакан сока ~250 мл, тарелка супа ~300 мл, порция каши ~250 г.",
    "- confidence_reasons — лаконично, без упоминаний баз и справочников.",
    "- Без советов, мотивации, оценок.",
    "",
    "Справка по порциям из базы данных (для оценки веса при «чашка», «ложка», «штука»):",
    "{{portion_hints}}",
  ].join("\n"),
};

const defaultSystemPrompt = [
  "Ты — вычислительный и аналитический модуль FoodMirror.",
  "Ты честно интерпретируешь еду и формируешь нейтральные наблюдения.",
  "Финальная арифметика калорий и БЖУ выполняется вне LLM.",
  "Отвечай только на русском языке, кроме поля canonical_name (английский lower_snake_case).",
  'ЗАПРЕЩЕНО: советы, мотивация, оценка пользователя, цели и нормы, слова: "нужно", "следует", "лучше", "стоит".',
  "ВСЕГДА: указывать неопределённость, возвращать строго один валидный JSON-объект без текста вокруг.",
].join("\n");

// Только секреты и идентификаторы окружения — из .env. Остальное зашито в конфиг.
const ENV = {
  telegramToken: process.env.TELEGRAM_BOT_TOKEN ?? "",
  databaseUrl: process.env.DATABASE_URL ?? "file:./dev.db",
  openaiApiKey: process.env.OPENAI_API_KEY ?? "",
  adminChatId: (process.env.ADMIN_CHAT_ID ?? "").trim(),
};

export const config = {
  telegramToken: ENV.telegramToken,
  databaseUrl: ENV.databaseUrl,
  reminderDefaults: {
    hour: 20,
    timezoneOffset: 180,
  },
  errorLogPath: "logs/error.log",
  llmLogPath: "logs/llm.log",
  llm: {
    provider: "openai" as const,
    apiKey: ENV.openaiApiKey,
    model: "gpt-5.2", // дефолт по проекту: gpt-5-mini; сменить здесь при смене модели
    timeoutMs: 45000,
    maxCompletionTokens: 1500,
    maxCompletionTokensDraftChat: undefined as number | undefined,
    systemPrompt: defaultSystemPrompt,
    contracts: defaultLlmContracts,
    prompts: defaultLlmPrompts,
  },
  draft: {
    maxMessages: 20,
    maxImages: 5,
  },
  subscription: {
    adminChatId: ENV.adminChatId,
    trialDays: 14,
    freeEstimatesPerDay: 3,
    freeConfirmsPerDay: 1,
    maxPhotosPerDay: 100,
    maxEstimateRequestsPerDay: 150,
  },
};
